const raio = 2

console.log(`
Logo o comprimento do círculo com o rai de ${raio}
é ${(2 * Math.PI * raio).toFixed(2)}cm
`);

console.log(`
Logo a área do círculo com o rai de ${raio}
é ${(Math.PI * (Math.pow(raio, 2))).toFixed(0)}cm²
`);